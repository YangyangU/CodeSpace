.hero{}
.hero image{
  height: 38.2vh;
}
.hero .content{
  padding-top:10%;
}
.hero .content.centered{
  text-align: center;
}
.hero .sub-header{
  font-size: 32rpx;
  text-transform: uppercase;
  letter-spacing: 6rpx;
  margin-bottom: 16rpx;
  color: rgba(0, 0, 0, 0.85);
}
.hero .header{
  font-size: 72rpx;
  font-weight: bold;
  margin: 0 12% 16rpx;/* 顺时针 */
}
.hero .description{
  padding: 0 12%;
  margin-bottom: 64rpx;
}