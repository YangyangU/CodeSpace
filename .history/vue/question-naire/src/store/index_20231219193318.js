import { createStore } from 'vuex'
import axios from 'axios'

export default createStore({
  state(){//全局数据源
    return {
      questionList:[]
    }
  },
  getters: { // computed

  },
  mutations: { // methods
  },
  actions: {
  },
  modules: {
  }
})
