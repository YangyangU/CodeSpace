s = "IX"

var romanToInt = function(s) {
    for (var i = 0; i < s.length; i++) {
        switch (s[i]) {
            case "I":1
            break
            case "V":5
        }
    }
};

console.log(romanToInt(s));