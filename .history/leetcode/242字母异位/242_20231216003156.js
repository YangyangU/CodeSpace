

var isAnagram = function(s, t) {
    let count1 = 0
    let count2 = 0
    for(const ch of s){
        count1 += ch.charCodeAt()
    }
    for(const ch of s){
        count1 += ch.charCodeAt()
    }
    if(s.length === t.length && )
};